def sign_with_csd(xml_bytes: bytes, cer_path: str = "", key_path: str = "", password: str = "") -> bytes:
    # Stub: solo devuelve el mismo XML. Sustituir por firmado real (RSA-SHA256)
    return xml_bytes
